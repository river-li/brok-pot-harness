# memory

The memory Host extension and its lifecycle-bound services. Read extension.ts for dependencies and registration.

Entry points and reference files: [memory-service.ts](memory-service.ts), [agent-state.ts](agent-state.ts), [extension.ts](extension.ts), [server-shard-sync.ts](server-shard-sync.ts), [memory-synthesis-service.ts](memory-synthesis-service.ts).

Build and launch from the [repository root](../../../../README.md). Current verification and
limitations are recorded in [migration status](../../../../MIGRATION_STATUS.md); copied tests or code
do not establish that this version has passed runtime verification.
