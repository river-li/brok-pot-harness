# inference

The inference Host extension and its lifecycle-bound services. Read extension.ts for dependencies and registration.

Entry points and reference files: [sand-labeling.ts](sand-labeling.ts), [voice-preview-service.ts](voice-preview-service.ts), [cursor-session.ts](cursor-session.ts), [extension.ts](extension.ts), [inference-service.ts](inference-service.ts), [transcribe-service.ts](transcribe-service.ts).

Build and launch from the [repository root](../../../../README.md). Current verification and
limitations are recorded in [migration status](../../../../MIGRATION_STATUS.md); copied tests or code
do not establish that this version has passed runtime verification.
