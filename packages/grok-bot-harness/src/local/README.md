# local

Responses, local identity, MCP/plugins, HTTP search/fetch, machine metadata and voice adapters.

Entry points and reference files: [web-search.ts](web-search.ts), [web-fetch.ts](web-fetch.ts), [mcp-scopes.ts](mcp-scopes.ts), [voice-server.ts](voice-server.ts), [desktop-mcp.ts](desktop-mcp.ts), [plugin-files.ts](plugin-files.ts).

Build and launch from the [repository root](../../../../README.md). Current verification and
limitations are recorded in [migration status](../../../../MIGRATION_STATUS.md); copied tests or code
do not establish that this version has passed runtime verification.
