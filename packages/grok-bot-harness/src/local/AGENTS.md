# local maintenance

Responses, local identity, MCP/plugins, HTTP search/fetch, machine metadata and voice adapters.

Follow the repository AGENTS.md. Use strict TypeScript and preserve the retained port contracts.
Keep keys out of renderer, speech services, tool subprocesses and diagnostics.
Changes must retain cancellation, limits and original approval behavior. Verify
with npm run check:local and the relevant contract/integration tests.
