init_zod();
var parametersSchema4 = external_exports.object({
  target_directory: external_exports.string().describe('The repository-relative path of the directory to list (e.g. "src/components"). Use "." or "" for the repository root.')
});
