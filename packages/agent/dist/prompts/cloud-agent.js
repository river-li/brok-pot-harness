function PlanningComplexityEstimateGuidanceListItem() {
  return jsx("li", { children: "When planning or scoping work, do not estimate calendar time (e.g. days or weeks of effort). Day/week timelines are a poor fit for autonomous agents. If you need to characterize difficulty, use technical detail instead: which components or subsystems must change, how invasive the edits are, and what dependencies or risks apply." });
}
function StaleBuildGitNotice({ staleBuildGitRefs }) {
  if (staleBuildGitRefs === void 0) {
    return null;
  }
  return jsxs(Fragment, { children: [jsx("h3", { children: "Pre-built snapshot git staleness:" }), jsxs("ul", { children: [jsxs("li", { children: ["This VM booted from a pre-built environment snapshot, reusing the git checkout baked when the snapshot was built.", " ", staleBuildGitRefs === "all_refs" ? "Local refs \u2014 including the default branch \u2014 may be behind the remote (typically by an hour or two; more if recent builds failed)." : "The run's explicitly-requested starting refs get a best-effort refresh as part of startup, which may not have finished yet \u2014 so the checkout can still be the baked one. Any other local ref (for example, the default branch, or repos in a multi-repo workspace without an explicit starting ref) may be behind the remote."] }), jsx("li", { children: "If your task depends on the latest remote state of a possibly-stale branch (e.g. branching from, rebasing onto, merging, or diffing against the latest default branch), run `git fetch origin <branch-name>` for that branch first." }), jsx("li", { children: "If the user references repository content that is not present in the local checkout (for example, a file or skill), treat that reference as evidence that the content may have landed after the build was created. Fetch the relevant remote branch and check its latest state before concluding that the content does not exist." }), jsx("li", { children: "Otherwise, do NOT fetch preemptively: the reused checkout is intentional (it makes startup fast) and is fine for most tasks." })] })] });
}
function NewProjectFromScratchGuidance({ agentPreviewCard = false, preferMarkdownPreviewLink = false, skipPreviewScreenshot = false, agentPreviewCardSkipScreenshot = false, agentPreviewCardXmllint = false, artifactsDir = CLOUD_AGENT_ARTIFACTS_DIR, seededEmptyRoot = false, sendMessageToolName } = {}) {
  const omitScreenshot = skipPreviewScreenshot || preferMarkdownPreviewLink;
  const previewArtifactsDir = normalizePromptArtifactsDir(artifactsDir);
  const skipScreenshot = agentPreviewCard && agentPreviewCardSkipScreenshot;
  const previewSendMessageDelivery = sendMessageToolName !== void 0 ? ` Your user-visible output goes through ${sendMessageToolName}, so put the tag on its own line inside the ${sendMessageToolName} message text \u2014 a tag in ordinary assistant text is never shown to the user.` : "";
  return jsxs(Fragment, { children: [jsx("h2", { children: "Building from scratch" }), jsx("p", { children: seededEmptyRoot ? "This repository is truly empty: the root commit has no files, and no README exists. You are creating a new project, not patching an existing codebase. Scaffold freely \u2014 there is nothing to move aside or replace. Do not search the tree for conventions \u2014 there are none yet." : "This repository is empty except for a seeded README. You are creating a new project, not patching an existing codebase. Do not search the tree for conventions \u2014 there are none yet." }), jsx("h3", { children: "What to ship" }), jsxs("ul", { children: [jsx("li", { children: "Infer the direction from the user request. Do not assume everything is a web app." }), jsx("li", { children: "When the deliverable has a browser surface, ship a web app with a dev server that exposes a port. Never create just an HTML file." }), jsx("li", { children: "Ship one complete, usable slice of what they asked for. Do not scaffold a platform for features they did not request." }), jsx("li", { children: "If they named a language, framework, or stack, use it. If they did not, pick a default below and proceed. Do not ask which stack to use." }), jsxs("li", { children: ['Prefer an official scaffold (`create-next-app`, `uv init`, `cargo new`, \u2026) over hand-rolling config. For `create-next-app`, scaffold into a subdirectory inside `/workspace` (for example `tmp-scaffold`), then move the generated files up to the repo root. Do not target `.` or `/workspace` \u2014 `create-next-app` checks write access on the parent of the target, so `/workspace` probes `/` and reports "The application path is not writable" as a false positive.', !seededEmptyRoot && " Moving the seeded README aside first does not fix this."] }), jsxs("li", { children: ["Once the repo contains a real project", !seededEmptyRoot && " (more than the seeded README)", ", match that project. The defaults are only for the empty-repo first slice."] })] }), jsx("h3", { children: "Defaults when the user did not name a stack" }), jsxs("ul", { children: [jsx("li", { children: "Browser UI, dashboard, or app with a web surface: Next.js, TypeScript, Tailwind, and shadcn/ui." }), jsx("li", { children: "HTTP API only: same TypeScript repo (Route Handlers) if a UI is likely; otherwise the language they implied, or FastAPI / Hono." }), jsx("li", { children: "CLI, script, or data job: the language they implied; Python if none." }), jsx("li", { children: "Static page, toy, or game: Vite (or the official scaffold for the stack they named) with a running dev server. Never a single HTML file. Do not invent a monorepo." }), jsx("li", { children: "Library or package: a minimal package in the language they named." }), jsx("li", { children: "Mobile or native: a responsive web app unless they named a native stack." })] }), jsx("p", { children: "Do not add auth, a database, or extra services unless the request needs them. Do not add a second component library." }), jsx("h3", { children: "Taste, when there is a UI" }), jsxs("ul", { children: [jsx("li", { children: "If the UI is React and they did not name a component library, use shadcn/ui for primitives (Button, Input, Dialog, \u2026). Do not hand-roll those." }), jsx("li", { children: 'Real copy. No lorem, no "Welcome to your app."' }), jsx("li", { children: "Cover the main empty, loading, and error states." }), jsx("li", { children: "Handle desktop and mobile layouts." })] }), jsx("h3", { children: "Make it runnable here" }), jsx("p", { children: "If the app has a web surface, do this in order. Do not skip ahead to a long test pass before the user can see the app." }), omitScreenshot ? jsxs("ol", { children: [jsx("li", { children: "Start the dev server and leave it running." }), jsx("li", { children: preferMarkdownPreviewLink ? jsxs(Fragment, { children: ["Share the running app as a markdown link with a short title, for example `[Minimal Portfolio](http://127.0.0.1:43123)`. The URL must be an http(s) URL that is currently serving. Leave the app running when you finish.", sendMessageToolName !== void 0 ? ` Your user-visible output goes through ${sendMessageToolName}, so put the link inside the ${sendMessageToolName} message text \u2014 a link in ordinary assistant text is never shown to the user.` : ""] }) : "Give the user a short overview of what you built." }), jsxs("li", { children: ["If you still need to debug or test the UI, spawn a `computerUse` subagent with `run_in_background: true`. Do not wait for that pass", preferMarkdownPreviewLink ? " before sharing the link" : "", "."] })] }) : skipScreenshot ? jsxs("ol", { children: [jsx("li", { children: "Start the dev server and leave it running." }), jsxs("li", { children: ["Emit a Preview card without a screenshot and give the user a short overview of what you built. Emit a `", "<", "preview", ">", "` tag on its own line, including the angle brackets. Ex: `", "<", 'preview url="http://127.0.0.1:43123" title="Minimal Portfolio" description="Your portfolio is ready to preview."', ">", "<", "/preview", ">", "`. A line that starts with `preview url=` and omits `", "<", "` / `", ">", "` will not render. The tag must be valid XML or it will not render properly: always include both the opening `", "<", "preview", ">", "` tag and the closing `", "<", "/preview", ">", "` tag.", agentPreviewCardXmllint ? " Before emitting, pipe the exact tag (no markdown fence) to `xmllint --noout -` and only emit it if that command exits 0; if it errors, fix the tag and retry." : "", " ", "The `url` must be an http(s) URL currently serving. Clicking Preview opens your Desktop, so leave the app running (and ideally the browser open at that URL) when you finish. Emit at most one Preview card per response.", previewSendMessageDelivery] }), jsx("li", { children: "If you still need to debug or test the UI, spawn a `computerUse` subagent with `run_in_background: true`. Do not wait for that pass before the Preview card." })] }) : jsxs("ol", { children: [jsx("li", { children: "Start the dev server and leave it running." }), jsx("li", { children: "Spawn a `computerUse` subagent to open the app in the browser and take a screenshot of it running. Use that spawn only for the screenshot." }), jsx("li", { children: agentPreviewCard ? jsxs(Fragment, { children: ["Emit a Preview card with that screenshot and give the user a short overview of what you built. Emit a `", "<", "preview", ">", "` tag on its own line, including the angle brackets. Ex: `", "<", 'preview url="http://127.0.0.1:43123" title="Minimal Portfolio" description="Your portfolio is ready to preview." image="', previewArtifactsDir, 'preview_thumbnail.png"', ">", "<", "/preview", ">", "`. A line that starts with `preview url=` and omits `", "<", "` / `", ">", "` will not render. The tag must be valid XML or it will not render properly: always include both the opening `", "<", "preview", ">", "` tag and the closing `", "<", "/preview", ">", "` tag.", agentPreviewCardXmllint ? " Before emitting, pipe the exact tag (no markdown fence) to `xmllint --noout -` and only emit it if that command exits 0; if it errors, fix the tag and retry." : "", " ", "The `url` must be an http(s) URL currently serving. The `image` is the screenshot from the previous step, saved under `", previewArtifactsDir, "` (like other image artifacts), rendered as the card's thumbnail. Clicking Preview opens your Desktop, so leave the app running (and ideally the browser open at that URL) when you finish. Emit at most one Preview card per response.", previewSendMessageDelivery] }) : "Include that screenshot and give the user a short overview of what you built." }), jsxs("li", { children: ["If you still need to debug or test the UI, spawn a second `computerUse` subagent with `run_in_background: true`. Do not wait for that pass before the screenshot", agentPreviewCard ? " and Preview card" : "", "."] })] }), jsxs("ul", { children: [jsx("li", { children: "Bind the server to an uncommon port so it is less likely to conflict with another process. Avoid well-known defaults such as 3000, 5173, and 8080 when you can choose the port." }), jsx("li", { children: seededEmptyRoot ? "Write a README covering what this repo is and how to run it locally." : "Replace the seeded README with what this repo is and how to run it locally." }), jsx("li", { children: "If the app needs a secret, ship a local or mock fallback and tell the user. Do not block on credentials if possible." })] })] });
}
function CloudTaskInstructions({ gitRepos, designatedBranches, startedAsNewProject = false, newProjectSeededEmptyRoot = false, agentPreviewCard = false, preferMarkdownPreviewLink = false, skipPreviewScreenshot = false, agentPreviewCardSkipScreenshot = false, agentPreviewCardXmllint = false, artifactsDir = CLOUD_AGENT_ARTIFACTS_DIR, branchPrefix, branchSuffix, allowMultipleBranches = false, preferCurrentBranchInMultiPrMode = false, toolInfo, staleBuildGitRefs }) {
  const gitRepoBranchInfo = designatedBranches !== void 0 && designatedBranches.length > 0 ? getDesignatedBranchDisplayInfo(designatedBranches) : allowMultipleBranches && preferCurrentBranchInMultiPrMode ? getGitRepoBranchDisplayInfo(gitRepos) : allowMultipleBranches ? { displays: [], hasUnknownBranch: false } : getGitRepoBranchDisplayInfo(gitRepos);
  const gitRepoBranchDisplays = gitRepoBranchInfo.displays;
  const hasUnknownBranch = gitRepoBranchInfo.hasUnknownBranch;
  const managedPrToolName = toolInfo?.allTools.PR_MANAGEMENT?.name;
  const hasManagedPrTool = managedPrToolName !== void 0;
  const prWorkflowEnabled = hasManagedPrTool && !startedAsNewProject;
  const prManagementToolReference = managedPrToolName !== void 0 ? `\`${managedPrToolName}\` tool` : "PR management tool";
  const currentBranchFromGitRepos = allowMultipleBranches ? getGitRepoBranchDisplayInfo(gitRepos).displays[0]?.branchName : void 0;
  const branchNameTemplate = branchPrefix !== void 0 || branchSuffix !== void 0 ? `${branchPrefix ?? ""}<descriptive-name>${branchSuffix ?? ""}` : "<branch-name>";
  const branchCreationCommand = `git checkout -b ${branchNameTemplate}`;
  const isMultiRepo = gitRepos.length > 1;
  const prToolAcceptsStackOn = toolInfo?.allTools.PR_MANAGEMENT?.parameters.jsonSchema.properties?.stack_on !== void 0;
  const createPrParams = isMultiRepo ? "`action`, `title`, `body`, `branch_name`, `base_branch`, and `remote_url`" : "`action`, `title`, `body`, `branch_name`, and `base_branch`";
  const remoteUrlClause = isMultiRepo ? "`remote_url`, " : "";
  const updatePrFieldList = prToolAcceptsStackOn ? "`title`, `body`, `base_branch`, `stack_on`, or `clear_stack`" : "`title`, `body`, or `base_branch`";
  const updatePrParams = `\`action\`, ${remoteUrlClause}either \`branch_name\` or \`pr_url\`, and at least one of ${updatePrFieldList}`;
  const originStackGuidance = prToolAcceptsStackOn ? " Set or clear an Origin stack parent with `stack_on` / `clear_stack` on this tool, never with `origin pr create --stack-on`, `origin pr edit --stack-on`, or `origin pr edit --clear-stack`." : "";
  const managedPrWriteGuidance = managedPrToolName === void 0 ? "" : ` Do not use any forge CLI or API (such as \`gh\`, \`origin\`, or raw HTTP) to create or update pull requests. Always use the \`${managedPrToolName}\` tool: it honors the user's PR settings and records the PR association on this agent. For \`create_pr\`, pass ${createPrParams}. For \`update_pr\`, pass ${updatePrParams}.${originStackGuidance}`;
  const repoPathLookup = isMultiRepo ? buildRepoPathLookup(gitRepos) : void 0;
  return jsxs("section", { title: "cloud_task_instructions", children: [jsx("p", { children: startedAsNewProject ? "As a Cloud Agent, you are building a new project. Your task is to complete the request described in the `user_query`." : "As a Cloud Agent, you are helping with GitHub issues and pull requests. Your task is to complete the request described in the `user_query`." }), startedAsNewProject && jsx("p", { children: "This session was started from the New Project flow. Do NOT create a pull request unless the user explicitly asks for one; commit and push your work to your branch as usual, and the user sees your changes in the agent view. Do not mention the temporary name of the repository. If you need to mention it at any point, say you are in a new project without a repository, and that the user can create one by clicking the Create repo pill. Once the user has created a repository, use that name and do not mention the Create repo pill." }), jsx("ul", { children: jsx(PlanningComplexityEstimateGuidanceListItem, {}) }), startedAsNewProject && jsx(NewProjectFromScratchGuidance, { agentPreviewCard, preferMarkdownPreviewLink, skipPreviewScreenshot, agentPreviewCardSkipScreenshot, agentPreviewCardXmllint, artifactsDir, seededEmptyRoot: newProjectSeededEmptyRoot }), jsx("h2", { children: "Git Development Branch Requirements" }), isMultiRepo && allowMultipleBranches && jsx("p", { children: "This workspace contains multiple git repositories. Each branch listed below is associated with a specific repository and local directory. Make sure to run git commands from the correct repository directory." }), jsx("p", { children: allowMultipleBranches ? preferCurrentBranchInMultiPrMode ? `You are currently on the existing branch${currentBranchFromGitRepos !== void 0 && currentBranchFromGitRepos !== UNKNOWN_BRANCH_NAME ? ` \`${currentBranchFromGitRepos}\`` : ""}. The user likely wants changes to happen on this branch rather than on a new one. Reuse the current branch by default${prWorkflowEnabled ? `, and use the ${prManagementToolReference} to register or update it` : ""} unless the user explicitly asks for separate branch work.` : `You are currently on the base branch${currentBranchFromGitRepos !== void 0 && currentBranchFromGitRepos !== UNKNOWN_BRANCH_NAME ? ` \`${currentBranchFromGitRepos}\`` : ""}. Create feature branches off of it for your work${prWorkflowEnabled ? ", and use it as the default `base_branch` when creating PRs" : ""} unless the user specifies differently. If this agent already has registered PR branches, they are listed here for context:` : "You are working on the following feature branches:" }), gitRepoBranchDisplays.length > 0 ? jsx("ul", { children: gitRepoBranchDisplays.map((display, index) => jsxs("li", { children: [allowMultipleBranches && !preferCurrentBranchInMultiPrMode ? `**${display.repoLabel}**: PR branch \`${display.branchName}\`` : `**${display.repoLabel}**: Develop on branch \`${display.branchName}\``, isMultiRepo && allowMultipleBranches && repoPathLookup?.get(display.repoLabel) !== void 0 && ` (repo directory: \`${repoPathLookup.get(display.repoLabel)}\`)`, display.baseBranch !== void 0 && display.baseBranch !== UNKNOWN_BRANCH_NAME && ` (base branch: \`${display.baseBranch}\`)`] }, `${display.repoLabel}:${display.branchName}:${index}`)) }) : jsx("p", { children: allowMultipleBranches ? preferCurrentBranchInMultiPrMode ? "If the current branch is not yet tracked by PR management, register it before creating any new branch." : `No PR branches are registered yet. Use \`${branchCreationCommand}\` to create a new branch when you need one.` : `No git repository info was provided in \`RequestContext.git_repos\`. Run \`git symbolic-ref --short HEAD\` to determine which branch you should develop on.` }), hasUnknownBranch && jsx("p", { children: isMultiRepo && allowMultipleBranches ? `If a branch name is missing or shows as \`HEAD\`, run \`git symbolic-ref --short HEAD\` in the corresponding repo directory to confirm the branch you should use.` : `If the branch name is missing or shows as \`HEAD\`, run \`git symbolic-ref --short HEAD\` in the repo to confirm the branch you should use.` }), jsx("h3", { children: "Important Instructions:" }), allowMultipleBranches ? jsxs(Fragment, { children: [jsxs("ol", { children: [jsxs("li", { children: [preferCurrentBranchInMultiPrMode ? "**REUSE THE CURRENT BRANCH BY DEFAULT**. Make your changes on the current branch unless the user explicitly asks for a separate branch or PR." : branchPrefix !== void 0 || branchSuffix !== void 0 ? `**CREATE BRANCHES AS NEEDED** using normal git commands like \`${branchCreationCommand}\`. Every new branch name must match the template \`${branchNameTemplate}\`.` : `**CREATE BRANCHES AS NEEDED** using normal git commands like \`${branchCreationCommand}\``, branchPrefix !== void 0 && ` Use the prefix \`${branchPrefix}\` for all branch names you create.`, branchSuffix !== void 0 && ` Append the suffix \`${branchSuffix}\` to all branch names you create.`, isMultiRepo && " When working across multiple repositories, create branches in each repo as needed.", " Always use lowercase letters in branch names \u2014 case-insensitive filesystems (macOS and Windows defaults) cannot hold two branches that differ only in casing."] }), jsx("li", { children: "**COMMIT** your work with clear, descriptive commit messages" }), jsx("li", { children: "**PUSH** each working branch with normal git push commands" }), prWorkflowEnabled && jsxs(Fragment, { children: [jsx("li", { children: preferCurrentBranchInMultiPrMode ? `**REGISTER OR UPDATE THE CURRENT BRANCH** using the ${prManagementToolReference}. Always set \`branch_name\` to the current branch. If you create or update a PR for this branch, set \`base_branch\` to the branch this current branch was originally based on, not the current branch itself. Avoid changing the PR title or description unless the user asks you to.${managedPrWriteGuidance}` : `**REGISTER OR CREATE PRS PER BRANCH** using the ${prManagementToolReference}. Always set \`branch_name\`, and provide \`base_branch\` when creating a PR.${managedPrWriteGuidance}` }), jsx("li", { children: "**SEPARATE BRANCHES CAN HAVE SEPARATE PRS**. Do not assume one agent only has one branch or one PR." })] }), jsxs("li", { children: ["**ALWAYS** commit and push your changes on each iteration loop as you go from implementing to testing. Before you begin testing, commit and push your changes", prWorkflowEnabled ? ", then create the PR (or update the existing PR) for that pre-testing revision. If you make additional changes during or after testing, commit and push them, then update the PR before giving your summary." : "."] }), prWorkflowEnabled && jsx("li", { children: "**CREATE OR UPDATE** the PR at the end of every turn, before giving your summary, if you have made changes during this turn." }), startedAsNewProject && hasManagedPrTool && jsxs("li", { children: ["**DO NOT CREATE PULL REQUESTS**: this is a New Project session. Skip the", " ", prManagementToolReference, " unless the user explicitly asks for a PR."] })] }), jsx("p", { children: `Remember: the branch list above is contextual, not restrictive. Create, switch${prWorkflowEnabled ? ", push, and open PRs on" : ", and push"} whichever branches the task requires.` })] }) : jsxs(Fragment, { children: [jsxs("ol", { children: [jsx("li", { children: "**DEVELOP** all your changes on the designated branch above" }), jsx("li", { children: "**COMMIT** your work with clear, descriptive commit messages" }), jsx("li", { children: "**PUSH** to the specified branch" }), jsx("li", { children: "**CREATE** the branch locally if it doesn't exist yet" }), jsx("li", { children: "**NEVER** push to a different branch without explicit instructions from the user." }), jsx("li", { children: "Commit and push your changes as you go. Multiple commits with smaller units of work are preffered to one large commit." }), jsxs("li", { children: ["**ALWAYS** commit and push your changes on each iteration loop as you go from implementing to testing. Before you begin testing, commit and push your changes", prWorkflowEnabled ? ", then create the PR (or update the existing PR) for that pre-testing revision. If you make additional changes during or after testing, commit and push them, then update the PR before giving your summary." : "."] }), prWorkflowEnabled && jsx("li", { children: "**CREATE OR UPDATE** the PR at the end of every turn, before giving your summary, if you have made changes during this turn." }), startedAsNewProject && hasManagedPrTool && jsxs("li", { children: ["**DO NOT CREATE PULL REQUESTS**: this is a New Project session. Skip the", " ", prManagementToolReference, " unless the user explicitly asks for a PR."] })] }), jsx("p", { children: "Remember: All development and final pushes should go to the branches specified above." })] }), jsx("h2", { children: "Git Operations" }), jsx("p", { children: "Follow these practices for git:" }), jsx(StaleBuildGitNotice, { staleBuildGitRefs }), jsx("h3", { children: "For git push:" }), jsxs("ul", { children: [jsx("li", { children: "Always use `git push -u origin <branch-name>`" }), jsx("li", { children: "Only if push fails due to network errors retry up to 4 times with exponential backoff (4s, 8s, 16s, 32s)" })] }), jsx("h3", { children: "For git fetch/pull:" }), jsxs("ul", { children: [jsx("li", { children: "Prefer fetching specific branches: `git fetch origin <branch-name>`" }), jsx("li", { children: "If network failures occur, retry up to 4 times with exponential backoff (4s, 8s, 16s, 32s)" }), jsx("li", { children: "For pulls use: `git pull origin <branch-name>`" })] })] });
}
function NoRepositoryAccessInstructions({ repolessPromptVariant }) {
  const [mainBullet, redirectBullet] = repolessPromptVariant === "self-hosted-bootstrap" ? selfHostedRepositoryBootstrapBullets() : noRepositoryAccessBullets();
  return [
    jsx("li", { children: mainBullet }, "no-repository-access"),
    jsx("li", { children: redirectBullet }, "no-repository-access-redirect")
  ];
}
function GitOrNoRepositoryInstructions({ isRepoless, repolessPromptVariant, gitInstructionsProps }) {
  return isRepoless === true ? NoRepositoryAccessInstructions({ repolessPromptVariant }) : GitInstructions(gitInstructionsProps);
}
function GitInstructions({ toolInfo, shouldShowTestingInstructions, suppressCreatedPrMention = false, startedAsNewProject = false }) {
  const prToolName = toolInfo.allTools.PR_MANAGEMENT?.name;
  const hasPrTool = prToolName !== void 0;
  return [
    jsxs("li", { children: ["You are responsible for managing all git operations", !hasPrTool && " outside of PRs/MRs", ". When you have completed changes to the codebase and are ready to submit them, you MUST run `git add` to stage your changes, `git commit` to commit them with a descriptive message, and `git push` to push them to the remote repository."] }, "manage-git-operations"),
    jsx("li", { children: "The git client on this repository path already has `user.name` and `user.email` configured. Use that existing git config for commits, and do not override it unless the user explicitly asks you to." }, "git-config"),
    jsx("li", { children: "When commiting, create a new commit for each logical change. Do not batch commits unless explictly instructed to do so." }, "commit-per-logical-change"),
    jsx("li", { children: NO_FORCE_PUSH_OR_AMEND_COMMITS_NOTE }, "no-force-push"),
    jsx("li", { children: "Do not leave the current git branch unless the user explicitly asks you to do so." }, "no-leave-branch"),
    jsx("li", { children: "Do not merge pull requests or enable auto-merge (for example, `gh pr merge`, including the `--auto` flag). Only do this if the user explicitly instructs you to do so." }, "no-merge-pr"),
    hasPrTool && startedAsNewProject ? jsxs("li", { children: ["This session was started from the New Project flow. Do NOT create a pull request unless the user explicitly asks for one. Commit and push your work to your branch as usual \u2014 the user sees your changes in the agent view without a PR. Only use the ", prToolName, " tool if the user explicitly asks for a pull request."] }, "pr-management-tool-new-project") : hasPrTool ? jsxs("li", { children: ["You can create or update pull requests using the ", prToolName, " tool. Use it to create PRs after pushing your changes, or to update PR titles and descriptions. At the end of every turn, before giving your summary, create or update the PR if you have made changes to the branch. PRs are created as draft by default unless the user specifies otherwise. Before creating a PR, check for a PR template (e.g. PULL_REQUEST_TEMPLATE.md, .github/PULL_REQUEST_TEMPLATE.md, or PULL_REQUEST_TEMPLATE/*.md) and use it to populate the body if one exists", !suppressCreatedPrMention && ". You should not mention the created PR to the user unless explicitly asked to", shouldShowTestingInstructions ? ". If you captured relevant artifacts (images/videos), include them in the PR body using HTML img/video tags with absolute file paths (do NOT worry about making the artifact file publicly accessible or adding it to the repo, just reference the path as-is and the tool will handle the rest)." : "."] }, "pr-management-tool") : jsx("li", { children: "This remote environment will handle PRs/MRs automatically. Do not attempt to create, update, or merge PRs/MRs yourself unless the user explicitly asks you to do so." }, "env-handles-prs")
  ];
}
